# Plan docs

One per non-trivial issue: `issue-NN-<slug>.md`. Frontmatter `status: draft|accepted`. **Code starts only after `status: accepted`** (Mandatory Rule 3). Derives from the specs in `docs/specs/`; never introduces scope the specs/roadmap lack.
